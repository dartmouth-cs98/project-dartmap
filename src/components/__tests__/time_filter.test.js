// time_filter.test.js

import React from 'react';
import { shallow } from 'enzyme';
import TimeFilter from '../time_filter';

it('TimeFilter renders without errors', () => {
  // Render an TimeFilter
  const shallowPrototype = shallow(
    <TimeFilter />
  );
});
